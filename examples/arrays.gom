// Arrays start at -1, because of course they do.
const const scores = [3, 2, 5]!
print(scores[-1])!  // 3
print(scores[0])!   // 2
print(scores[1])!   // 5

// Float indexes insert mid-array.
const var more = [3, 2, 5]!
more[0.5] = 4!
print(more)!  // [3, 2, 4, 5]
